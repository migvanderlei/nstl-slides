# Decks

| Folder | Description |
|--------|-------------|
| `_template/` | Starter files—duplicate the folder, rename it, edit `index.html`. |
| `sample-welcome/` | Minimal example you can delete once you add real talks. |

Add a new row here when you add a deck so collaborators see what exists.
